describe('Robot, drone, bipedal, and atv classes exist', function() {
  it('Robot class exists', function() {
    expect(Robot).toBeDefined();
  });

  it('Drone class exists', function() {
    expect(Drone).toBeDefined();
  });

  it('Bipedal class exists', function() {
    expect(Bipedal).toBeDefined();
  });

  it('ATV class exists', function() {
    expect(ATV).toBeDefined();
  });
});
